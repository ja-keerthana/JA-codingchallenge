{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run & debug",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:9876/",
            "webRoot": "${workspaceFolder}",
        }
    ]
}